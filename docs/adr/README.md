# Architecture Decision Records

One file per decision, numbered, never edited after acceptance; a superseding
decision gets a new file that links back. Write them for a reader who was not in the room.

Format: **Context** (the forces), **Decision** (one sentence, then detail),
**Consequences** (what becomes easier, what becomes harder), **Status**.
